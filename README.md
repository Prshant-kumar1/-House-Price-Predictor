# -House-Price-Predictor
This project aimed to build a Streamlit web application for house price prediction.  The core idea was to:  Train a machine learning model (specifically, a Linear Regression model) using historical housing data. Integrate the model into the web application to take user inputs, preprocess them, and then predict the house price dynamically. 
